name: Codex Improve

on:
  workflow_dispatch:

jobs:
  improve:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run Codex
        run: |
          echo "Codex workflow running"
